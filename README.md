# .NET Core 3 & Postgres Repository
A .NET Core Web API with Postgres Repository pattern

## Getting started
To get you the database running just execute the following in cmd/bash:
```
docker-compose up
```